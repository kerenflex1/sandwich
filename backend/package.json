{
  "name": "backend",
  "version": "1.0.0",
  "description": "Simple messaging backend with miniapp support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo 'No tests' && exit 0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
